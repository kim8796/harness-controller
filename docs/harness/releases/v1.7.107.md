# Harness Release v1.7.107

Release date: 2026-05-12

This patch polishes the external controller read-only target smoke shipped in v1.7.106. It does not add aliases, RootContext-aware product execution, commits, or pushes.

## Changes

- Clarified CLI help, generated target dashboard guidance, and controller bundle README so `target run --once` is described as read-only/no-op smoke.
- Kept product-changing autonomy lane execution explicitly disabled until a later RootContext-aware execution phase.
- Added `target-detached-head` as a `target run --once` blocker.
- Added the smoke step to controller clone/use docs.

## Non-Goals

- No target alias/default selector.
- No Telegram/Redis grammar or signing change.
- No product-changing lane execution, commit, push, PR, or merge.

## Verification

- Focused target CLI/controller/export tests.
- Controller export check and exported controller focused tests.
- Harness run validation and pre-push guard.
