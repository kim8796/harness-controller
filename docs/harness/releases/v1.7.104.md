# Harness Release v1.7.104

Date: 2026-05-12

## Summary

This patch adds target-scoped lock safety for external controller target run preflight.

## Changes

- Added `targets/<id>/locks/target-run.lock` as the per-target run lock.
- Made `./harness target run <id> --once` acquire and release that lock during preflight.
- Kept external target execution fail-closed; no product-changing lane starts.
- Did not change Telegram grammar, Redis relay keys, or product repo mutation behavior.

## Validation

- `python3 -m pytest tests/test_harness_controller.py tests/test_harness_cli.py -q`
- `python3 -m ruff check scripts/harness_controller.py scripts/harness_cli.py tests/test_harness_controller.py tests/test_harness_cli.py`
- `python3 scripts/harness_export.py --check`
- Controller export sanitization and focused exported pytest smoke.
- `python3 scripts/harness_guard.py --mode pre-push --run-lint --run-pytest`
