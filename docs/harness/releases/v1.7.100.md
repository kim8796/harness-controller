# Harness Release v1.7.100

Date: 2026-05-12

## Summary

This patch completes the controller export seed surface by including the focused CI test files needed by the controller workflow.

## Changes

- Added controller-only export of `tests/test_harness_cli.py`, `tests/test_harness_controller.py`, and `tests/test_harness_export.py`.
- Added a generated controller-specific `tests/conftest.py` that provides only `load_script_module`, avoiding product app settings imports in the standalone controller repo.
- Updated the controller GitHub Actions workflow to use controller-aware export checks and run the exported controller bundle's focused pytest suite.
- Kept starter bundles free of controller CI workflow/test files.

## Safety Notes

- The controller bundle still excludes `.env*`, `targets/**`, live autonomy/report state, and generated `exports/**`.
- External target lane execution remains fail-closed pending RootContext-aware autonomy execution.

## Validation

- `.venv/bin/python -m pytest tests/test_harness_export.py tests/test_harness_cli.py tests/test_harness_controller.py -q`
- `.venv/bin/python -m ruff check scripts/harness_export.py scripts/harness_cli.py scripts/harness_controller.py scripts/harness_autonomy/core.py tests/test_harness_export.py tests/test_harness_cli.py tests/test_harness_controller.py`
- `python3 scripts/harness_export.py --check`
- `python3 scripts/harness_export.py --check --controller-bundle <tmp-controller>`
- Temp controller export smoke with controller-bundle focused pytest passing.
