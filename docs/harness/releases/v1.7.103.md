# Harness Release v1.7.103

Date: 2026-05-12

## Summary

This patch hardens the external controller path contract by introducing a single `StatePaths` resolver for each target.

## Changes

- Added `StatePaths` for `target_id`, `controller_root`, `target_root`, `state_root`, and target-scoped sidecar paths.
- Routed target registry/dashboard path projection through the resolver.
- Kept `./harness target run <id> --once` fail-closed and product-repo-clean.
- Did not change Telegram grammar, Redis relay envelopes, target execution, or product commit/push behavior.

## Validation

- `python3 -m pytest tests/test_harness_controller.py tests/test_harness_cli.py -q`
- `python3 -m ruff check scripts/harness_controller.py scripts/harness_cli.py tests/test_harness_controller.py tests/test_harness_cli.py`
- `python3 scripts/harness_export.py --check`
- Controller export sanitization and focused exported pytest smoke.
- `python3 scripts/harness_guard.py --mode pre-push --run-lint --run-pytest`
